# 🚨 Экстренное восстановление доступа к админке

## Проблема
- Забыли пароль от админки
- Ваши учетные записи не входят как админ
- Нет коллекции users в Firestore

## ✅ Решение за 2 минуты

### Вариант 1: Добавить ВРЕМЕННЫЙ email администратора

1. **Откройте файл** `frontend/src/ProtectedRoute.jsx`

2. **Добавьте специальный временный email:**

```javascript
const ADMIN_EMAILS = [
    'vadim5239@gmail.com',
    'vadimlavrenchuk@yahoo.com',
    'admin@kontrollitud.ee',
    'temp@admin.ee'  // ← ДОБАВЬТЕ ЭТУ СТРОКУ
];
```

3. **Пересоберите frontend:**

```bash
cd frontend
npm run build
```

Или если используете Docker:

```bash
docker compose up -d --build frontend
```

4. **Зарегистрируйтесь на сайте с email `temp@admin.ee`** (любой пароль)

5. **Перейдите на страницу `/admin`** - у вас будет доступ!

---

### Вариант 2: Узнать текущие email и добавить их

1. **Откройте Firebase Console:**
   - https://console.firebase.google.com
   - Выберите проект `kontrollitud-ee`

2. **Перейдите в Authentication:**
   - Слева в меню: Authentication → Users
   - Посмотрите список всех зарегистрированных пользователей
   - Найдите свои 2 учетные записи и скопируйте их email

3. **Добавьте эти email в код:**

Откройте `frontend/src/ProtectedRoute.jsx` и добавьте реальные email:

```javascript
const ADMIN_EMAILS = [
    'vadim5239@gmail.com',
    'vadimlavrenchuk@yahoo.com',
    'admin@kontrollitud.ee',
    'your-real-email@example.com',  // ← ВАШ РЕАЛЬНЫЙ EMAIL
    'your-second-email@example.com' // ← ВАШ ВТОРОЙ EMAIL
];
```

4. **Пересоберите и перезапустите:**

```bash
docker compose up -d --build frontend
```

---

### Вариант 3: Сбросить пароль через Firebase

1. **Откройте Firebase Console:**
   - https://console.firebase.google.com
   - Authentication → Users

2. **Найдите свою учетную запись**

3. **Нажмите три точки ⋮ → Reset password**

4. **Проверьте email** - придет ссылка для сброса пароля

5. **После сброса пароля** - убедитесь, что ваш email есть в `ADMIN_EMAILS` в коде

---

## 🎯 Быстрая команда для перезапуска

```powershell
# Остановить все контейнеры
docker compose down

# Пересобрать и запустить
docker compose up -d --build

# Проверить логи
docker compose logs -f frontend
```

---

## 📝 Проверка текущих администраторов

Текущие email-адреса администраторов в коде:
- `vadim5239@gmail.com`
- `vadimlavrenchuk@yahoo.com`
- `admin@kontrollitud.ee`

Если ваши учетные записи имеют другие email - просто добавьте их в список!
