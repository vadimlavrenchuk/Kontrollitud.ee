# Настройка Firestore Rules для Kontrollitud.ee

## 🔥 Проблема

Ошибка `auth/invalid-credential` при попытке создать компанию через форму. Это происходит из-за строгих правил безопасности Firestore.

## ✅ Решение

### Вариант 1: Обновить правила через Firebase Console (РЕКОМЕНДУЕТСЯ)

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект **kontrollitud-a015c**
3. Перейдите в **Firestore Database** → **Rules**
4. Скопируйте содержимое файла `firestore.rules` из корня проекта
5. Вставьте в редактор правил
6. Нажмите **Publish**

### Вариант 2: Временно открыть доступ (ТОЛЬКО ДЛЯ ТЕСТИРОВАНИЯ!)

Если нужно быстро протестировать, используйте эти правила:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ВРЕМЕННО: разрешить всем авторизованным пользователям писать в pending_companies
    match /pending_companies/{document=**} {
      allow read, write: if request.auth != null;
    }
    
    match /companies/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    match /{document=**} {
      allow read: if true;
    }
  }
}
```

⚠️ **ВНИМАНИЕ:** Вариант 2 небезопасен для production! Используйте только для тестирования.

### Вариант 3: Использовать Firebase CLI

```bash
# Установите Firebase CLI
npm install -g firebase-tools

# Войдите в Firebase
firebase login

# Инициализируйте проект (если еще не сделали)
firebase init firestore

# Деплой правил
firebase deploy --only firestore:rules
```

## 🔒 Что делают правила из firestore.rules

### Коллекция `pending_companies`:
- ✅ Любой авторизованный пользователь может создавать заявки
- ✅ Пользователи видят только свои заявки
- ✅ Админы видят все заявки
- ✅ Только админы могут удалять заявки

### Коллекция `companies`:
- ✅ Все могут читать одобренные компании
- ✅ Только админы могут редактировать/удалять

### Коллекция `reviews`:
- ✅ Все могут читать отзывы
- ✅ Только авторизованные могут создавать отзывы
- ✅ Только автор или админ могут редактировать/удалять свои отзывы

### Коллекция `admins`:
- ✅ Только админы имеют доступ
- ✅ Проверка роли через документ в коллекции admins

## 🧪 Проверка после обновления правил

1. Обновите страницу (Ctrl+F5)
2. Авторизуйтесь
3. Попробуйте создать компанию
4. Проверьте Firebase Console → Firestore → pending_companies

Компания должна появиться в коллекции `pending_companies`!

## 📝 Создание первого админа

После обновления правил, создайте первого админа вручную:

1. Firebase Console → Firestore Database
2. Создайте коллекцию `admins`
3. Добавьте документ с ID = `UID вашего пользователя`
4. Добавьте поле: `role` (string) = `"admin"`

Теперь этот пользователь будет иметь права админа!
