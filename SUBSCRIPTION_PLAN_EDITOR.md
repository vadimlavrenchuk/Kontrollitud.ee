# Редактор Тарифного Плана в Форме Редактирования Компании

## ✅ Реализовано

Добавлена возможность изменения тарифного плана подписки прямо в форме редактирования компании.

## 📋 Изменения

### Frontend

#### 1. EditCompany.jsx
- Добавлено состояние `subscriptionLevel` в formData
- Добавлено состояние `trialInfo` для отображения информации о пробном периоде
- Добавлена секция "Подписка" с карточками планов (Basic, Pro, Enterprise)
- Карточки показывают:
  - Название плана
  - Цену
  - Список функций
  - Текущий выбранный план
  - Пробный период (если активен)
- `handleSubmit` теперь отправляет `subscriptionLevel` на сервер

#### 2. EditCompany.scss
- Добавлены стили для секции подписки:
  - `.subscription-section` - общий контейнер
  - `.trial-notice` - уведомление о пробном периоде с градиентом
  - `.plan-cards` - сетка карточек планов
  - `.plan-card` - стили для карточки плана с hover эффектами
  - `.current-plan-badge` - бейдж текущего плана
  - `.plan-notice` - предупреждение об оплате
- Карточки с разными цветами границ при выборе:
  - Basic: фиолетовый (#667eea)
  - Pro: синий (#3b82f6)
  - Enterprise: желтый (#fbbf24)

#### 3. i18n.js
Добавлены переводы на 3 языка (RU/ET/EN):
- `subscription_plan` - Тарифный план
- `current_plan` - Текущий план
- `trial_active` - Пробный период активен
- `trial_expires` - Истекает
- `month` - месяц/kuu/month
- Функции планов:
  - `basic_listing`, `basic_profile`, `customer_reviews`
  - `all_basic_features`, `priority_placement`, `analytics_dashboard`
  - `social_media_links`, `custom_branding`
  - `all_pro_features`, `dedicated_support`, `api_access`
  - `custom_integrations`, `priority_support`
- `plan_change_billing_info` - уведомление об оплате

### Backend

#### server.js
- Роут `PUT /api/user/companies/:id` теперь принимает `subscriptionLevel`
- `updateData.subscriptionLevel` обновляется если передан в запросе
- Поддержка обновления trial статуса сохранена

## 🎨 UI/UX Особенности

### Секция "Подписка"
1. **Уведомление о пробном периоде** (если активен):
   - Зеленый градиентный блок
   - Показывает дату окончания пробного периода
   - Эмодзи 🎉 для привлечения внимания

2. **Карточки планов**:
   - Адаптивная сетка (3 колонки на десктопе)
   - Hover эффект: подъем карточки + тень
   - Выбранная карточка: цветная граница + увеличенная тень
   - Бейдж "Текущий план" в правом верхнем углу
   - Цена крупным шрифтом
   - Список функций с чекмарками ✓

3. **Интерактивность**:
   - Клик по карточке выбирает план
   - Анимации переходов (0.3s ease)
   - Визуальная обратная связь

4. **Предупреждение об оплате**:
   - Желтый блок с иконкой 💡
   - Показывается только для платных планов
   - Не показывается во время пробного периода

## 📱 Адаптивность

- Карточки планов автоматически перестраиваются на мобильных устройствах
- Минимальная ширина карточки: 280px
- Grid система `auto-fit` для плавной адаптации

## 🔄 Логика Работы

1. **Загрузка данных**:
   - При загрузке компании получаем текущий `subscriptionLevel`
   - Получаем trial статус (`trialActive`, `trialEndDate`, `trialUsed`)
   - Устанавливаем в состояние формы

2. **Изменение плана**:
   - Пользователь кликает на карточку плана
   - `subscriptionLevel` обновляется в `formData`
   - Визуально выделяется выбранная карточка

3. **Сохранение**:
   - При submit формы `subscriptionLevel` отправляется на сервер
   - Backend обновляет план в MongoDB
   - Если выбран платный план - показывается предупреждение об оплате

4. **Trial период**:
   - Если активен - показывается зеленое уведомление
   - Не показывается предупреждение об оплате во время trial
   - Trial статус сохраняется при изменении плана

## 🎯 Преимущества Решения

1. ✅ **Удобство**: Можно изменить план не покидая форму редактирования
2. ✅ **Наглядность**: Все планы видны одновременно с функциями
3. ✅ **Информативность**: Trial статус всегда виден
4. ✅ **Предупреждение**: Пользователь знает об оплате до сохранения
5. ✅ **Консистентность**: Тот же дизайн что и на странице pricing
6. ✅ **Многоязычность**: Полная локализация на 3 языках

## 🔧 Технические Детали

### Структура данных
```javascript
formData: {
  name: string,
  phone: string,
  email: string,
  website: string,
  // ...
  subscriptionLevel: 'basic' | 'pro' | 'enterprise'
}

trialInfo: {
  trialActive: boolean,
  trialEndDate: Date | null,
  trialUsed: boolean
}
```

### API Request
```javascript
PUT /api/user/companies/:id
Content-Type: multipart/form-data

{
  name: "...",
  subscriptionLevel: "pro",
  // ... other fields
}
```

### CSS Classes
- `.subscription-section` - контейнер секции
- `.trial-notice` - уведомление о trial
- `.plan-cards` - grid контейнер
- `.plan-card` - карточка плана
- `.plan-card.selected` - выбранная карточка
- `.current-plan-badge` - бейдж текущего плана
- `.plan-notice` - предупреждение об оплате

## 📝 Следующие Шаги (Опционально)

1. **Интеграция с платежами**:
   - При смене на платный план - редирект на страницу оплаты
   - Подтверждение оплаты перед сохранением

2. **История изменений**:
   - Лог изменений тарифного плана
   - Отображение в админ-панели

3. **Уведомления**:
   - Email уведомление при смене плана
   - Push уведомление о необходимости оплаты

4. **Аналитика**:
   - Отслеживание конверсии изменений планов
   - A/B тесты разных дизайнов секции

## ✨ Результат

Теперь пользователи могут:
- ✅ Видеть свой текущий тарифный план
- ✅ Видеть статус пробного периода
- ✅ Менять план прямо в форме редактирования
- ✅ Понимать что потребуется оплата
- ✅ Сравнивать планы и их функции
